# Customizacje Klimabazar (fork chatwoot/chatwoot)

Fork: `adayet/chatwoot`, gałąź `klimabazar`. Przypięta do stabilnego tagu upstream
(start: **v4.14.1**, commit `d58b6a6`).
Build: GitHub Actions → `ghcr.io/adayet/chatwoot`. Serwer pobiera gotowy obraz (nie kompiluje).

## Reguła sidecar-first
Co da się zrobić jako sidecar / feature-flag / custom-attribute / inicjalizator-wolumen
→ NIE wchodzi do forka. Forkujemy głównie front Vue. Backendy danych (BaseLinker, wFirma)
żyją w osobnym repo `adayet/chatwoot-integrations`.

## Marker zmian
Każda edycja w źródłach oznaczona `KLIMABAZAR` (grep-owalne).

## Rejestr zmian
| Plik | Zmiana | Po co | Kruchość przy upgrade | Jak zweryfikować |
|---|---|---|---|---|
| `.github/workflows/build-image.yml` | nowy workflow build→GHCR | własny obraz bez kompilacji na prod | niska (plik własny) | run zielony, obraz w GHCR |
| `vite.config.ts` | env-gated `server.allowedHosts` (flaga `VITE_ALLOW_ALL_HOSTS`) | lokalny dev w Dockerze (Vite 5.4 blokuje Host `vite` → 403 → blank screen); no-op bez flagi, prod bez dev-servera | niska (no-op domyślnie) | `curl localhost:3001/vite-dev/@vite/client` = 200, dashboard renderuje |
| `ChatList.vue` (l.~76, ~365), `store/.../conversations/index.js` (l.~15) | domyślny filtr listy `OPEN` → `ALL` | F6: rozwiązane nie znikają z widoku | średnia (linie mogą się przesunąć — grep `KLIMABAZAR F6`) | wejście na listę → filtr „Wszystkie" |
| `composables/useConversationHandlingState.js` (nowy) | wspólna logika stanu obsługi (nowy/w toku/rozwiązane/uśpione/oczekujące) → klasy `n-*` | F6: jedno źródło prawdy dla obu kart listy | niska (plik własny) | jednostkowo: „w toku" TYLKO gdy `first_reply_created_at` > 0 (odpowiedź do klienta); samo przypisanie agenta nie zmienia stanu — ticket zostaje „nowy" |
| `ConversationCard.vue` (legacy/domyślny) + `components-next/.../ConversationCardExpanded.vue` | wariant C: neutralne tło; status = **prawy** pasek (osobny element absolutny, NIE border — bo reguła Chatwoota `[&>div:has(+ div .active)>*]:!border-n-surface-1` szarzy bordery wiersza nad aktywnym) + chip (prawy górny róg); active = **lewy** niebieski pasek (border-l) + tło slate-3; hover = slate-2 | F6: widać status, odróżnia nieobsłużone od obsługiwanych; lewa=active, prawa=status; obie karty | średnia (struktura kart) | Nowy=bez koloru, W toku=amber, Rozwiązane=teal, Oczekujące=ruby, Uśpione=slate; pasek statusu zachowany też nad aktywnym wierszem |
| `ChatList.vue` (F1) | kolejność zakładek assignee: Wszystkie→Moje→Nieprzypisane (sort `ASSIGNEE_TAB_ORDER`) + domyślnie aktywna zakładka = Wszystkie | F1: właściciel chce widzieć całość od wejścia | niska (1 plik) | zakładki w tej kolejności; po wejściu aktywna „Wszystkie" |
| `ChatList.vue` (F7, Droga B) | agregowana lista „Rozmowy" wyklucza rozmowy z etykietami `HIDDEN_LABELS` (`['spam','kurier','marketplace','powiadomienia']`); pomija gdy folder/filtr, widok etykiety (`props.label`) LUB konkretny inbox (`activeInbox`) | F7: szum znika z domyślnego widoku (po F6 „rozwiąż" już nie chowa), ale zostaje dostępny w `#etykieta`/folderze/inboxie/szukajce | niska–średnia (computed `conversationList`); licznik zakładki nieskorygowany (kosmetyka) | rozmowa z `spam`/`kurier`/`marketplace` znika z „Rozmowy", widoczna w widoku etykiety i w swoim inboxie (np. allegro@) |
| `ConversationCard.vue` + `MessagePreview.vue` (F2) | F2: agent (avatar + neutralna nazwa) przeniesiony do prawego górnego rogu, na lewo od chipu statusu, oddzielony kropką; podgląd wiadomości w 2 liniach (prop `multiline` w MessagePreview, domyślnie 1 — bez wpływu na inne użycia) | „kto prowadzi" przy statusie + więcej kontekstu wiadomości | średnia (karta + współdzielony MessagePreview) | agent z avatarem • chip w prawym górnym rogu; podgląd do 2 linii |
| `components-next/sidebar/Sidebar.vue` (F3) | `visibleMenuItems` filtruje `menuItems` po `name` wg `KLIMABAZAR_HIDDEN_SIDEBAR` (`['Captain','Reports','Campaigns']`) | F3: ukryć Captain, **Raporty i Kampanie**. UWAGA: flagi konta `reports`/`campaigns` **NIE** bramkują tych pozycji w tej wersji (trasy `*_index` mają `meta` bez `featureFlag`, a `shouldShow` jest domyślnie `true` na self-hosted) — dlatego ukrywamy je filtrem, nie flagą | niska–średnia (struktura sidebara) | Captain/Raporty/Kampanie znikają z nawigacji |
| `i18n/locale/{en,pl}/chatlist.json` | blok `CHAT_LIST.HANDLING_STATE` (5 etykiet) | F6: etykiety badge; `pl` bo instalacja PL-only | niska | badge po polsku w UI |
| `app/views/api/v1/conversations/partials/_conversation.json.jbuilder` (F8, **backend-fork: czulszy przy mergach upstreamu**) | +`chat_messages_count` (`messages.chat.count`) i `last_chat_message_at` (`messages.chat.maximum(:created_at)`); scope `Message.chat` = bez aktywności i notatek prywatnych | F8: liczba realnych wiadomości i czas ostatniej realnej wiadomości dla karty listy | średnia (serializer bywa zmieniany) | render partiala ma oba pola; `last_chat_message_at` ≤ `last_activity_at` (aktywność nie rusza daty) |
| `components-next/.../ConversationCard/MessageCountBadge.vue` (F8, nowy) | neutralny (`n-slate`) licznik liczby wiadomości; zastępuje `UnreadBadge` w kartach (UnreadBadge nietknięty); pokazywany **gdy > 1** (1 wiadomość = bez badge) | F8: badge w rogu = liczba wiadomości wątku, nie nieprzeczytane | niska (plik własny) | szary badge z liczbą gdy wątek ma >1 wiadomość |
| `components/ui/TimeAgo.vue` (F8, **współdzielony — czulszy przy mergach**) | prop `singleTimestamp` (domyślnie false): gdy true renderuje tylko `lastActivityTime` zamiast `utworzono • aktywność`; reszta bez zmian | F8: karty pokazują tylko datę ostatniej realnej wiadomości (jedna wartość) | średnia (komponent upstream) | karta pokazuje jedną godzinę = ostatnia realna wiadomość |
| `dashboard/i18n/locale/pl/*.json` (i18n pl, **WYSOKA kruchość — pl jest własnością forka**) | uzupełnianie pełnego tłumaczenia pl panelu agenta (braki + nieprzetłumaczone); nazwy własne po EN, polska liczba mnoga (3 formy), placeholdery nienaruszone. **Stan: Tier Wysoki (22 pliki, codzienne ekrany agenta) gotowy; Tier Średni/Niski w toku** (spec/plan: `i18n-pl-coverage-design.md`/`-plan.md`) | pełne pl panelu (upstream Crowdin pokrywa ~53%) | **wysoka** (upstream aktualizuje pl) — przy mergu brać wersję forka + dolać nowe klucze i przetłumaczyć | `python3 scripts/klimabazar/check_i18n_pl.py` (raport + per-plik OK) |
| `scripts/klimabazar/check_i18n_pl.py` (nowy) | kontrola QA tłumaczeń: parytet kluczy en↔pl + integralność placeholderów (suma po segmentach mnogiej) + raport pokrycia | weryfikacja tłumaczeń, przydatne przy mergach upstreamu | niska (plik własny) | uruchomienie skryptu |
| `app/models/concerns/sort_handler.rb` + `app/finders/conversation_finder.rb` (F9, **backend-fork: czulszy przy mergach**) | scope `sort_on_last_message_at` (`MAX(created_at)` wiadomości `message_type<>2 AND private=false`, NULLS LAST) + **domyślny** sort listy = `last_message_at_desc`; `# rubocop:disable Metrics/BlockLength` na bloku `class_methods` | F9: lista domyślnie po ostatniej realnej wiadomości, nie po `last_activity_at` (notatki/przypisania nie podbijają) | średnia (finder/sort bywają zmieniane) | konsola: aktywność nie zmienia kolejności; nowa wiadomość podbija |
| `globals.js` + `store/.../conversations/helpers.js` + `ChatList.vue` + `ConversationBasicFilter.vue` (F9) | `SORT_BY_TYPE.LAST_MESSAGE_AT_*` + komparator `sortOnLastMessageAt` (po `last_chat_message_at` z F8, brak→na dół) + nowe domyślne (3 miejsca) + pozycja w dropdownie sortu | F9: front spójny z backendem (live-update + wybór) | średnia (kilka plików) | dropdown ma „Ostatnia wiadomość"; domyślnie aktywna |
| `i18n chatlist.json` en+pl (F9) | etykiety `SORT_ORDER_ITEMS.last_message_at_desc/asc` | F9: nazwa opcji sortu | niska | etykieta w menu |
| **(deploy, nie kod)** reset `User.ui_settings.conversations_filter_by.order_by` (F9) | jednorazowo po deployu — wymusza nowy domyślny sort na wszystkich (dalej mogą wybrać własny) | F9: „wszyscy domyślnie po ostatniej wiadomości" | n/d | po deployu lista każdego użytkownika sortuje po ostatniej wiadomości |
| `CardContent.vue` + `ConversationCardExpanded.vue` (F8) | prop `chatMessagesCount` → `MessageCountBadge`; `TimeAgo` z `last_chat_message_at \|\| timestamp`; `unreadCount` zostaje (kolor podglądu) | F8: karta expanded — licznik + data ostatniej realnej | średnia (karta) | badge=liczba wiadomości; data nie skacze po przypisaniu agenta |
| `components/widgets/conversation/ConversationCard.vue` (F8) | `UnreadBadge`→`MessageCountBadge` (`chat.chat_messages_count`); `TimeAgo` z `last_chat_message_at \|\| timestamp`; `hasUnread` zostaje (pogrubienie wiersza) | F8: karta legacy/condensed — licznik + data ostatniej realnej | średnia (karta) | jw. w layoucie condensed; nieprzeczytane dalej przez pogrubienie |
| `contextMenu/Index.vue` + `ConversationItem.vue` + `ChatList.vue` + `AddAutomationRule.vue` + `i18n conversation.json` en+pl (F-auto) | menu kontekstowe konwersacji: pozycja „Dodaj nową automatyzację" (tylko admin) → modal reguły z prefillem (zdarzenie `conversation_created`, warunki `email`/`mail_subject` z konwersacji, akcja `add_label` pusta); reużycie modala z Ustawień, zapis przez `automations/create`; `AddAutomationRule.open(prefill)` opcjonalny (bez argumentu = jak dotąd) | F-auto: szybsze tworzenie reguł anty-spam z poziomu listy | średnia (pliki upstream: menu/karta/lista) — grep `KLIMABAZAR F-auto` | prawy przycisk na konwersacji e-mail → pozycja widoczna (admin) → modal z prefillem email+temat i pustą akcją label; zapis → reguła w Ustawieniach → Automatyzacja |
| `components/widgets/conversation/ConversationCard.vue` (F-prio) | priorytet jako kolorowy chip (ikona `i-woot-priority-*` + etykieta) w prawym górnym rogu, obok chipa stanu (wariant C); kolory spójne z F6: urgent=ruby, high=amber, medium=blue, low=slate; etykiety z istniejących kluczy `CONVERSATION.PRIORITY.OPTIONS.*`. Zastępuje domyślną `CardPriorityIcon` w rzędzie nazwy skrzynki, która po F2 chowała się pod blokiem agenta (niewidoczna) | F-prio: priorytet widoczny na aktualnym listingu | średnia (karta, grep `KLIMABAZAR F-prio`) | konwersacja z ustawionym priorytetem → kolorowy chip obok chipa stanu; brak priorytetu = brak chipa |
| `shared/helpers/KeyboardHelpers.js` + `composables/useKeyboardEvents.js` (F-altgr, **współdzielony — czulszy przy mergach**) | helper `isAltGraphPressed` (`getModifierState('AltGraph')`, pomija Apple) + guard w `shouldIgnoreEvent` ignorujący skróty gdy wciśnięty AltGr; na Windows AltGr=Ctrl+Alt, więc polskie litery (ą=AltGr+A) trafiały w skróty `Alt+*`/`$mod+Alt+*` — m.in. `$mod+Alt+KeyA`=Dodaj załącznik odpalał się przy pisaniu „ą". Uzupełnia istniejący fix QWERTZ (`keysToModifyInQWERTZ`), który obejmował tylko niemiecki | F-altgr: pisanie polskich znaków diakrytycznych (ą/ć/ę/ł/ń/ó/ś) nie odpala skrótów; kandydat na PR do upstreamu | niska–średnia (2 pliki, grep `KLIMABAZAR F-altgr`) | Win+Firefox/PL: AltGr+A w polu wiadomości wpisuje „ą", NIE otwiera załącznika; zwykłe Ctrl+Alt+A dalej dodaje załącznik; Mac bez zmian |
| `app/finders/conversation_finder.rb` (F-rozw, **backend-fork: czulszy przy mergach**) | gałąź `status=unresolved` → `where.not(status: :resolved)`; obsługuje listę i meta-licznik (wspólny finder) | F-rozw: zakładka „Nierozwiązane" = wszystko poza resolved | średnia (finder bywa zmieniany) — grep `KLIMABAZAR F-rozw` | konsola: `ConversationFinder` z `status:'unresolved'` zwraca `count` = liczba ≠ resolved; `resolved + unresolved == all` |
| `ChatList.vue` (F-rozw) | rząd 5 zakładek: Wszystkie/Moje/Nieprzypisane (status=all) + Rozwiązane/Nierozwiązane (assignee=all), wykluczające; `listTabItems`/`activeListTab`/`updateListTab`; liczniki niezależne `fetchTabCounts` (3× `meta`) zamiast singletona `conversationStats` dla badge'y | F-rozw: szybki dostęp do rozwiązanych i nierozwiązanych z poziomu rzędu zakładek | średnia (rząd zakładek + handler) — grep `KLIMABAZAR F-rozw` | rząd ma 5 zakładek z liczbami; status wykluczający; dropdown synchronizuje podświetlenie |
| `i18n/locale/{en,pl}/chatlist.json` (F-rozw) | klucze `ASSIGNEE_TYPE_TABS.resolved`/`.unresolved` (etykiety zakładek) + `CHAT_STATUS_FILTER_ITEMS.unresolved.TEXT` (etykieta chipa statusu w nagłówku, inaczej renderuje surowy klucz) | F-rozw: etykiety nowych zakładek i chipa | niska | „Rozwiązane"/„Nierozwiązane" w rzędzie zakładek i w chipie nagłówka |
| `store/.../conversations/helpers.js` (F-rozw, **współdzielony — czulszy przy mergach**) | `filterByStatus`: `'unresolved'` → `chatStatus !== 'resolved'` (filtr kliencki listy/live-update; bez tego lista jest pusta mimo licznika 6) | F-rozw: kliencki filtr listy rozumie „nierozwiązane" | średnia (helper współdzielony) — grep `KLIMABAZAR F-rozw` | zakładka „Nierozwiązane" pokazuje wszystkie poza resolved (live-update też) |
| `components/widgets/ChatTypeTabs.vue` (F-tabord) | `vuedraggable` wokół `woot-tabs-item` (`delay:200`=przytrzymanie startuje drag, krótki klik=przełącz; **`force-fallback:true` KONIECZNE — `delay` NIE działa z natywnym HTML5 DnD, tylko z fallbackiem pointer/mouse**; `class="contents"` nie psuje flexa; `ghost-class="opacity-40"`); **`@choose`→ sygnał uniesienia** (klucz trzymanej zakładki → `scale-110` + `shadow-md`, `transition-all`); emit `reorder` z kolejnością kluczy | F-tabord: użytkownik przeciąga zakładki, z wizualnym sygnałem złapania | średnia (komponent tab-bara, używany tylko w ChatList) — grep `KLIMABAZAR F-tabord` | przytrzymanie unosi zakładkę (scale+cień) i pozwala przestawić; krótki klik dalej przełącza |
| `ChatList.vue` (F-tabord) | `DEFAULT_TAB_ORDER` + `orderedTabItems` (sort wg `uiSettings.conversation_tabs_order`, brak→domyślna, klucze spoza zapisu na koniec) + `onTabsReorder`→`updateUISettings`; bind `:items="orderedTabItems"` + `@reorder` | F-tabord: kolejność zakładek per-user z ui_settings | niska–średnia — grep `KLIMABAZAR F-tabord` | kolejność zostaje po reloadzie; inny user ma własną |
| `app/views/layouts/_kb_boot_fallback.html.erb` (F-boot, nowy) + `layouts/vueapp.html.erb` (render partiala) + `entrypoints/dashboard.js` (F-boot) | obrona warstwowa przed „białym ekranem" SPA: (1) inline **ES5 + inline-style** fallback w layoucie — loader (usuwany wspólnym MutationObserverem na `#app` w partialu — gdy SPA wyrenderuje dzieci; działa dla wszystkich packów: dashboard, v3app/login), a jeśli panel nie wstanie w 45 s (lub padnie `<script>`) pokazuje nakładkę „Nie udało się załadować panelu" + przycisk Odśwież + **userAgent** (diagnostyka wersji bez konsoli); (2) handler `vite:preloadError`→`location.reload` raz (guard `sessionStorage`, anty-pętla 10 s) dla starych chunków 404 po deployu. **Świadomy wyjątek od „Tailwind only / no inline styles"** — fallback musi działać, gdy bundle JS/CSS się nie załadował (np. stary Firefox/Windows nie parsuje bundla) | F-boot: zamiast bieli user widzi komunikat i może odświeżyć; kandydat na PR do upstreamu (i18n/EN przed PR) | niska–średnia (layout + entry, grep `KLIMABAZAR F-boot`); testy Playwright/Firefox: `/tmp/kb-boot-test` (4 scenariusze) | DevTools: gdy SPA nie montuje → nakładka w ≤45 s; gdy montuje → brak nakładki; po deployu otwarta karta + lazy route → auto-reload raz |

| `dashboard/i18n/index.js` (**przepisany — upstream-owy, czulszy przy mergach**) + `entrypoints/dashboard.js` + `entrypoints/v3app.js` + `dashboard/App.vue` + `settings/profile/UserLanguageSelect.vue` + `settings/account/Index.vue` (F-i18n) | lazy-load tłumaczeń: zamiast statycznego importu 41 języków (~7 MB w chunku startowym) tylko EN (fallback) w bundlu, reszta przez jawną mapę `() => import('./locale/xx')` (chunk-per-język). `createI18n({ messages:{en}, fallbackLocale:'en' })`; entrypointy doczytują aktywny locale (`selectedLocale`) przed `mount` (loader F-boot pokrywa await); `setLocale`/UserLanguageSelect/account doczytują locale przed przełączeniem. Mapa jawna (nie `import.meta.glob`) bo katalogów locale jest 56, a kompletnych 41 (glob wciągał niekompletne, np. `zh`, i wywracał build) | F-i18n: −~7 MB ze startowego chunku (12,27→~5 MB), reszta przyczyny białego ekranu; kandydat na PR do upstreamu. Spec: `docs/klimabazar/F-i18n-lazy-locale-{design,plan}.md` | **średnia–wysoka** (6 plików, `index.js` upstream-owy — grep `KLIMABAZAR F-i18n`) | po buildzie: chunk startowy bez 40 języków (`grep -c SEARCH_PLACEHOLDER` ~1, nie 1643); osobne chunki per język; PL na starcie; przełączanie języka doczytuje i działa |

| `dashboard/api/channel/voice/twilioVoiceClient.js` (F-twilio, **upstream-owy — czulszy przy mergach**) | `@twilio/voice-sdk` (~0,6–1 MB) z importu statycznego na górze → dynamiczny `await import('@twilio/voice-sdk')` w `initializeDevice()` (już async, wołana dopiero przy starcie rozmowy). `Device` używany tylko tam | F-twilio: SDK poza chunkiem startowym — osobny chunk ładowany tylko przy pierwszej rozmowie (Klimabazar: WhatsApp/e-mail, rozmowy rzadkie → zwykle nie ładuje się wcale); kandydat na PR do upstreamu | niska (1 plik, grep `KLIMABAZAR F-twilio`) | po buildzie: `@twilio` w osobnym chunku, nie w starcie; rozmowa głosowa nadal działa (SDK doczytany przy inicjacji) |

| `shared/helpers/BaseActionCableConnector.js` (F-wswake, **współdzielony — czulszy przy mergach**) | po `visibilitychange`→visible i `window focus` wymusza `consumer.connection.reopen()`, gdy socket nie jest otwarty LUB okno było ukryte > `WAKE_REOPEN_THRESHOLD` (10 s); guard `lastReopenAt` przeciw podwójnemu reopen; nasłuchy sprzątane w `disconnect()`. reopen przechodzi przez istniejący łańcuch `disconnected → checkConnection → onReconnect → sync`, więc dociągnięcie wiadomości nie jest duplikowane | F-wswake: naprawia „zombie connection" w zainstalowanej PWA — po uśpieniu/tle przeglądarka zamraża JS, ActionCable nie wykrywa martwego socketu, a serwer go nie zamyka (wisi za NAT) → agent ma nieaktualne wiadomości do ręcznego „Odśwież". Potwierdzone w logach: połączenie otwarte 32 min bez dostarczania. Kandydat na PR do upstreamu | niska–średnia (1 plik, grep `KLIMABAZAR F-wswake`) | DevTools: zminimalizuj/uśpij okno > 10 s, wróć → w konsoli reopen socketu i dociągnięcie nowych wiadomości bez ręcznego reloadu; serwer: brak długich „otwartych, niedostarczających" sesji w `ws-health.sh` |

| `store/.../conversations/helpers.js` (F9-fix, **współdzielony — czulszy przy mergach**) | `sortOnLastMessageAt`: fallback `last_chat_message_at \|\| timestamp \|\| created_at \|\| 0` (wszystko epoch). Bez tego sort F9 psuł **live-update listy**: pole `last_chat_message_at` dostarcza tylko API (jbuilder), a eventy WS (`conversation.created`/`message.created`) go nie niosą i `ADD_MESSAGE` aktualizuje tylko `timestamp` → nowe/zaktualizowane konwersacje miały sort-key 0 i spadały na dół listy (znikały z góry do ręcznego „Odśwież") | F9-fix: lista odświeża się na żywo (nowa konwersacja/wiadomość wskakuje na górę bez reloadu); spójne z kartami (`last_chat_message_at \|\| chat.timestamp`) | średnia (1 plik, grep `KLIMABAZAR F9`) | wyślij testowego maila na skrzynkę → nowa konwersacja pojawia się na górze „Wszystkie" bez ręcznego odświeżania; spec `helpers.spec.js` zielony |

| `components-next/.../ConversationCardExpanded.vue` + `components/widgets/conversation/ConversationCard.vue` (F-unread) | niebieska kropka nieprzeczytanej rozmowy (`bg-n-blue-9`) jak w Sparku. Expanded: element flex `w-2 h-2` po lewej obok avatara nadawcy (warunek `unreadCount > 0`). Condensed: `absolute top-2 left-1 w-2.5 h-2.5 z-[2]` w lewym górnym rogu karty (warunek `hasUnread`). Uzupełnia istniejące pogrubienie nazwy, nie zastępuje | F-unread: szybciej widać nieprzeczytane na liście | niska–średnia (2 karty, grep `KLIMABAZAR F-unread`) | rozmowa z `unread_count > 0` → niebieska kropka (expanded: obok avatara; condensed: lewy górny róg); przeczytana = brak kropki |

## Cykl wciągania upstreamu
1. `git fetch upstream --tags`
2. Wybierz kolejny stabilny tag `vX.Y.Z` (NIE `develop`).
3. `git merge vX.Y.Z` do `klimabazar`; rozwiąż konflikty (grep `KLIMABAZAR`).
4. Build CI → test lokalny (Docker, parytet).
5. **Backup Postgresa** (`pg_dump -Fc chatwoot_production`), potem deploy: `pull → db:migrate → up -d`.
6. Po deployu sprawdź pozycje z kolumny „Jak zweryfikować" powyżej.

## Stan produkcji (referencja)
- Serwer: Hetzner `178.105.179.180` (Ubuntu 24.04, 4 GB RAM), deployment w `/opt/chatwoot`.
- Postgres: `pgvector/pgvector:pg16`, baza `chatwoot_production`, user `postgres`.
- Compose: serwis `base` (obraz dziedziczony przez `rails`/`sidekiq`), `postgres`, `redis`.
- Łatka wolumenowa: `patches/zz_html_signature.rb` (kolorowy podpis w mailach) — sprawdź log `patch applied`.

## Twarde reguły
- Nigdy `docker compose down -v`.
- `pg_dump` przed każdym deployem z migracją lub mergem upstreamu.
- Build tylko w CI — serwer 4 GB nie udźwignie kompilacji.
